import { StoryObj, Meta } from '@storybook/react'
import { Checkbox, CheckboxProps } from '@designer-ui/react';

export default {
  title: 'Form/Checkbox',
  component: Checkbox,
  args: {}
} as Meta<CheckboxProps>

export const Checked: StoryObj<CheckboxProps> = {

}